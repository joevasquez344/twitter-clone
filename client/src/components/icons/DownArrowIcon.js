import React from "react";

const DownArrowIcon = () => {
  return (
    <i style={styles.downArrowIcon} className="fas fa-chevron-down"></i>
  );
};

const styles = {
  downArrowIcon: {
    color: "white",
  },
};

export default DownArrowIcon;

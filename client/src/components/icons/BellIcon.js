import React from "react";

const BellIcon = () => {
  return <i style={styles.bellIcon} className="fas fa-bell fa-2x"></i>;
};

const styles = {
  bellIcon: {
    color: "white",
  },
};

export default BellIcon;

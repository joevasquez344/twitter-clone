import React from "react";

const HomeIcon = () => {
  return <i style={styles.homeIcon} className="fas fa-home fa-2x"></i>;
};

const styles = {
  homeIcon: {
    color: "white",
  },
};

export default HomeIcon;

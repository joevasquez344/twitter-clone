import React from "react";

const HashTagIcon = () => {
  return <i style={styles.hashtag} className="fas fa-hashtag fa-2x"></i>;
};

const styles = {
  hashtag: {
    color: "white",
  },
};

export default HashTagIcon;

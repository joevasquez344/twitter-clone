import React from "react";

const ProfileIcon = () => {
  return <i style={styles.profileIcon} className="far fa-user fa-2x"></i>;
};

const styles = {
  profileIcon: {
    color: "white",
  },
};

export default ProfileIcon;
